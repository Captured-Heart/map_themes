/// Layout options for the theme selector
enum ThemeSelectorLayout {
  /// Dropdown button layout
  dropdown,

  /// Horizontal scrollable list of chips
  horizontalList,

  /// Grid layout with chips
  grid,
}
